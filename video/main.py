from App import *

if __name__ == '__main__':
    app = App()
    app.mainloop()
    # file = input_video_val.get()
    # #video_dim = 
    # #fps = 
    # lineStart = tuple(origin_coord_val.get())
    # lineEnd = tuple(dest_coord_val.get())
    # threshold = int(threshold_val.get())
    # #showVideo = 
    # tracker = VehicleCounter(file, video_dim,fps, lineStart,lineEnd, threshold, showVideo)